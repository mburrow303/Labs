/*
1. Add two strings together that use the same type of quotes

   e.g. `'Java' + 'Script'`

3. Add two strings together using different types of quotes

   e.g. `"I'm double quoted!" + 'I am single quoted.'`
   
6. Subtract two strings
  
   e.g. `'cats' - 'dogs'`
   
9. Add a string to a number
    
11. What happens when you try to put quotes inside your string's quotes?

Write your code below here, remember to use `console.log(someValue)`.
*/
console.log('slap' + 'shot');
console.log("the Dude Abides " + "Walter doesn't roll on shabbos");
console.log('bunny' - 'toe');
console.log(4 + " nihilists");

// with quotes inside of string's quotes the code wont run correctly

// You can find the length of a string using string.length
// You can convert to uppercase using text.toUpperCase();
// You can retrieve the first letter of a string using text.charAt(0);